---
/** Крошки. Все ссылки — настоящие <a> в статическом HTML, без JS. */
const { crumbs = [] } = Astro.props;
---
{crumbs.length > 1 && (
  <nav class="crumbs" aria-label="Breadcrumb">
    <ol>
      {crumbs.map((c, i) => (
        <li>
          {i < crumbs.length - 1 ? <a href={c.href}>{c.label}</a> : <span aria-current="page">{c.label}</span>}
        </li>
      ))}
    </ol>
  </nav>
)}
